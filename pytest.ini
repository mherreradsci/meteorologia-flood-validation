[pytest]
# Sin pyproject.toml/setup.py en este repo (ver CLAUDE.md): src/ se agrega
# al sys.path acá en vez de instalar el paquete, mismo criterio que
# PYTHONPATH=src para correr flood_validation manualmente.
pythonpath = src
testpaths = tests
